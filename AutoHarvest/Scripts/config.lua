local Config = {
    QueryFrequency = 100, -- ms between collision tests
    Distance = 220, --
    UseSphereMethodToSearch = true, -- Uses a faster sphere collision test rather than iterating all plants.
    PrintDebugMessages = true,
}

return Config